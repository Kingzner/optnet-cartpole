# Python
__pycache__/
*.py[cod]
*.so

# Environments
.venv/
venv/
env/

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Job logs (cluster runs)
output.*.out
error.*.err
*.log

# Intermediate files produced by the scripts
cartpole_linear_dt001.npz
constrained_vs_unconstrained_results.pkl
quick_constrained_vs_unconstrained.pkl
outputs/horizon/table_results.csv

# Note: the dataset (data/), the training results (outputs/) and the figures
# (figures/) are intentionally tracked, so that every figure and table in the
# thesis can be reproduced without re-training or re-generating the dataset.
